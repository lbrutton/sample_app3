<li>
	<span class="content"><%= micropost.content %></span> <!-- my guess is that if you have render @microposts (plural) in the 
show.html.erb view, rails automoatically knows to use it as an array, and add each elemant to a |micropost| block
variable -->
	<span class="timestamp">
		Posted <%= time_ago_in_words(micropost.created_at) %> ago.
	</span>
  <% if current_user?(micropost.user) %>
    <%= link_to "delete", micropost, method: :delete,
                                     data: { confirm: "You sure?" },
                                     title: micropost.content %>
  <% end %>
</li>